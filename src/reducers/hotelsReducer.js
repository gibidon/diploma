import { ACTION_TYPE } from '../actions';

const initialHotelsState = {};

export const hotelsReducer = (state = initialHotelsState, action) => {
	switch (action.type) {
		default:
			return state;
	}
};
